version: 2

models:
  - name: dim_sales_creditcard
    description: Tabela final da dimensão de cartões de crédito
    columns:
      - name: pk_credit_card
        description: Chave primária da tabela final de informações de cartão de crédito
        tests:
          - unique
          - not_null
  - name: ft_sales_order_reason
    description: Tabela final fato de junção da tabela de pedidos com a tabela de motivos do pedido
    data_tests:
          - dbt_utils.unique_combination_of_columns:
              arguments:
                combination_of_columns:
                  - fk_sales_order
                  - pk_sales_reason
  - name: ft_sales_order
    description: Tabela final fato dos pedidos
    columns:
      - name: pk_sales_order
        description: Chave primária da tabela final fato dos pedidos
        tests:
          - not_null
      